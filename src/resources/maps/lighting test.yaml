format on next load: false

size: [100, 100]

script: 
    type: default

global:
    gravity: 10

objects:
  - type: spawn
    ID: playerSpawn
    checkCollision: ALL
    spawnInt: 0
    position: [-.5, .6, -.5]
  
  - type: static
    physics:
        file: plane
    position: [0, 0, 0]
    orientation: [0, 0, 0, 1]